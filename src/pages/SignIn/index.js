import React from 'react';
import { View } from 'react-native';

import { Container, BigText } from './styles';

export default function SignIn() {
    return (
        <Container>
            <BigText>Default project</BigText>
        </Container>
    );
}
